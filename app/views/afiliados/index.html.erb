<h3 class="subheader">Afiliados</h3>
<hr />
<table class="large-12" role="grid">
	<thead>
		  <tr>
		  	<th>Número Afiliado</th>
		  	<th>Apellido y Nombre </th>
		  	<th>Tipo	      </th>
		  	<th>Documento	      </th>
		    <th>&nbsp;</th>
		  </tr>
    </thead>
    <tbody>
    <% @afiliados.each do |afiliado| %>
		  <tr>
		  	<td><%= afiliado.apellido_nombre %>        </td>
		  	<td><%= afiliado.numero_afiliado %>        </td>
		  	<td><%= afiliado.tipo_documento %>         </td>
		  	<td><%= afiliado.numero_documento %>       </td>
		    <td><%= link_to 'Ver', afiliado, {:class => "button small" } %>     </td>
		  </tr>
    <% end %>
   </tbody>
</table>